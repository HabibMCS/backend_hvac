// Spinner.jsx
import React from 'react';
import './Spinner.css'; // Importing the CSS for the spinner

const Spinner = () => {
    return <div className="spinner"></div>;
};

export default Spinner;
